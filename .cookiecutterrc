# This file exists so you can easily regenerate your project.
#
# `cookiepatcher` is a convenient shim around `cookiecutter`
# for regenerating projects (it will generate a .cookiecutterrc
# automatically for any template). To use it:
#
#    pip install cookiepatcher
#    cookiepatcher gh:itisfoundation/cookiecutter-osparc-service project-path
#
# See:
#    https://pypi.python.org/pypi/cookiepatcher
#
# Alternatively, you can run:
#
#    cookiecutter --overwrite-if-exists --config-file=project-path/.cookiecutterrc gh:itisfoundation/cookiecutter-osparc-service
#

default_context:

    _output_dir:               '/home/lmoyasans/repos'
    _template:                 'gh:ITISFoundation/cookiecutter-osparc-service'
    author_affiliation:        "IT'IS Foundation"
    author_email:              'moyasans@itis.swiss'
    author_name:               'Lucia Moya-Sans'
    contact_email:             'moyasans@itis.swiss'
    default_docker_registry:   'itisfoundation'
    docker_base:               'custom:special-image'
    git_repo:                  'github'
    git_username:              'lmoyasans'
    number_of_inputs:          '1'
    number_of_outputs:         '1'
    project_name:              'k-Wave'
    project_package_name:      'k_wave'
    project_short_description: 'A MATLAB toolbox for the time-domain simulation of acoustic wave fields. It uses the kspaceFirst3DG function to run 3D time-domain simulation of wave propagation on a GPU using C++ CUDA code.'
    project_slug:              'k-wave'
    project_type:              'computational'
    release_date:              '2023'
    version:                   '2.0.0'
